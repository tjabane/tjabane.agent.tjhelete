{
  "name": "@tjabane-agent-tjhelete/repository",
  "version": "0.1.0",
  "private": true,
  "description": "Persistence repository module for Agent Tjhelete.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "npm run build && node --test test/*.test.mjs",
    "test:coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-include=dist/**/*.js test/*.test.mjs"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@azure/cosmos": "^4.10.0"
  }
}
